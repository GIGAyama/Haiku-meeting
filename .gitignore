node_modules/

# tools/build.mjs が Tailwind CLI に書かせる中間ファイル。
# 配信するのは css.html のほうなので、こちらは追跡しない。
tools/tailwind-output.css

# clasp（GASのプッシュ設定。スクリプトIDが入るので置かない）
.clasp.json
.clasprc.json

# 反映前のGASの控え（CIの成果物として持ち出すので、リポジトリには置かない）
dist/gas-before-push/
